// DataDashboard
// Web Application

const dataDashboard = {
   setup: function() {
      library.ui.autoDisableButtons();
      function onLoadSetup() {
         }
      $(onLoadSetup);
      }
   };

dataDashboard.controller = {
   };

dataDashboard.setup();
