// DataDashboard
// Web Application

const dataDashboard = {
   setup: () => {
      library.ui.autoDisableButtons();
      const onLoadSetup = () => {
         };
      $(onLoadSetup);
      }
   };

dataDashboard.controller = {
   // Controller
   };

dataDashboard.setup();
